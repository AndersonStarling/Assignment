#include <stdio.h>
#include <stdlib.h>

float acreage_Rectangle(float length, float width)
{
    float acreage = length * width;
    printf("test program);
    printf("Tao conflict");
    return acreage;
}
